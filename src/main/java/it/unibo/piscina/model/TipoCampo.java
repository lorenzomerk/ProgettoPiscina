package it.unibo.piscina.model;

/** Tipo logico usato dai form generici dei moduli di dominio. */
public enum TipoCampo {
    TESTO,
    TESTO_LUNGO,
    INTERO,
    DECIMALE,
    DATA,
    ORA,
    DATA_ORA,
    BOOLEANO,
    ELENCO,
    RIFERIMENTO
}
